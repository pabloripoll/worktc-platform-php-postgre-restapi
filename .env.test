# Define your env variables for the test env here
KERNEL_CLASS='App\Kernel'
APP_SECRET='$ecretf0rt3st'
SYMFONY_DEPRECATIONS_HELPER=999999
PANTHER_APP_ENV=panther
PANTHER_ERROR_SCREENSHOT_DIR=./var/error-screenshots

# Database - Use "wtc" as base name (Symfony will add "_test" suffix automatically)
DATABASE_URL="postgresql://devuser:devpass@192.168.1.41:7700/wtc?serverVersion=17&charset=utf8"

# Redis - Use different database index for tests
REDIS_DSN=redis://rootuser:rootpass@192.168.1.41:7701/1

# MongoDB - Use test database
MONGODB_URL=mongodb://rootuser:rootpass@192.168.1.41:7702
MONGODB_DATABASE=wtc_test
MONGODB_USERNAME=rootuser
MONGODB_PASSWORD=rootpass

# Messenger - Use in-memory transport for tests
MESSENGER_TRANSPORT_DSN=doctrine://default

# Mailer - Use null transport for tests (no emails sent)
MAILER_DSN=null://null

# JWT
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=your_passphrase
JWT_TTL=3600
